<% content_for :submenu do %>
  <%= render "submenu" %>
<% end %>

<% title t('pages.edit.title') %>
<% breadcrumbs_admin link_to(t('pages.index.title'), pages_path),
                     link_to(t('pages.edit.title'), edit_page_path(@page) ) %>

<div class="cols-container">
  <div class="main-column">
    <%= render 'edit_form' %>
  </div>
  <div class="tooltip-column">
    <%= render 'tooltip' %>
  </div>
</div>

<%= link_to t('back'), pages_path %>
