<% content_for :submenu do %>
  <%= render "submenu" %>
<% end %>

<% title t('.title') %>
<% breadcrumbs_admin link_to(t('l.news.index.title'), news_index_path), link_to(t('.title'), new_news_path) %>

<div class="cols-container">
  <div class="main-column">
    <%= render 'form' %>
  </div>
  <div class="tooltip-column">
    <%= render 'tooltip' %>
  </div>
</div>

<%= link_to t('back'), news_index_path %>
