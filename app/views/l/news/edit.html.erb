<% content_for :submenu do %>
  <%= render "submenu" %>
<% end %>

<% title t('news.edit.title') %>
<% breadcrumbs_admin link_to(t('news.index.title'), news_index_path),
                     link_to(t('news.edit.title'), edit_news_path(@news)) %>

<div class="cols-container">
  <div class="main-column">
    <%= render 'edit_form' %>
  </div>
  <div class="tooltip-column">
    <%= render 'tooltip' %>
  </div>
</div>

<%= link_to t('back'), news_index_path %>
