try {
  var row = $("input:checkbox[value=<%= @user.id %>]")
    .parents()
    .slideUp(function() {
      $(this).remove()
    });
    
} catch(err) {
  if (console != undefined) {
    console.log(err);
  }
}

