<% content_for :title do t('.title') end %>
<% breadcrumbs_admin link_to(t('l.admin.galleries.index.title'), admin_galleries_path),
  link_to(t('.title'), edit_admin_gallery_path) %>

<%= content_tag(:p, flash[:notice], class: :notice) if flash.key? :notice %>
<%= content_tag(:p, flash[:alert], class: :alert) if flash.key? :alert %>


<div class="cols-container">
  <div class="main-column">
    <%= render 'edit_form' %>
  </div>
  <div class="tooltip-column">
    <%= render 'tooltip' %>
  </div>
</div>

<%= link_to t('helpers.actions.back'), admin_galleries_path %>
