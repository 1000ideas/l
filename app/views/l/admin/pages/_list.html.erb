<%= form_for :selection, url: '#', html: {method: :post} do |f| %>
  <ul class="items-list <%= 'filtered' if filtering? %>" data-sortable-update="<%= sort_admin_pages_path %>">
    <%= render partial: 'page', collection: @pages, locals: {f: f} %>
  </ul>
<% end %>
