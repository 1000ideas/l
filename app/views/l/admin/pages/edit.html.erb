<% title t('.title') %>
<% breadcrumbs_admin link_to(t('l.admin.pages.index.title'), admin_pages_path), link_to(t('.title'), edit_admin_page_path(@page) ) %>

<div class="cols-container">
  <div class="main-column">
    <%= render 'edit_form' %>
  </div>
  <div class="tooltip-column">
    <%= render 'tooltip' %>
  </div>
</div>

<%= link_to t('helpers.actions.back'), admin_pages_path %>
